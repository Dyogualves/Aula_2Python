#Exercicios de tuplas

# # 1 -  Crie uma tupla chamada `frutas` com pelo menos 5 frutas diferentes. Em seguida, acesse e imprima o terceiro elemento da tupla.


# tupla = ('abacaxi', 'uva', 'laranja', 'morango', 'goiaba')

# print(tupla[2])


# # 2 - Crie uma tupla chamada `numeros` com alguns números inteiros. Em seguida, converta essa tupla em uma lista e imprima a lista resultante.

# numeros = (1,2,3,4,5,6,7,8,10)
# n = list(numeros)

# print(n)

# # 3 - Crie uma tupla chamada meses com os nomes dos meses do ano até Agosto. Use um loop for para imprimir cada mês em uma linha separada


# meses = ('janeiro', 'fevereiro', 'março', 'abril', 'maio','junho', 'julho', 'agosto')

# for i in (meses):
#     print(i)

# # 4 - Crie uma lista chamada `notas` com algumas notas de alunos. Em seguida, converta essa lista em uma tupla e imprima a tupla resultante. Faça a média das notas.


# notas = [8,10,9,10,7]

# n= tuple(notas)

# soma = sum(notas)/9

# print(n)
# print ('a media das notas é:', (soma))

# #5 - Crie uma lista chamada `ponto` que represente as coordenadas (x, y, e z) de um ponto. Em seguida, desempacote os elementos da lista em duas variáveis separadas (x ,y e z ) e imprima os valores.

# ponto = [325, 657, 297]

# x,y,z = ponto

# print(x, y, z )
